# 🚀 Mobile App - Setup Guide for Emergency Alerts\n\n## Step 1: Update Login Handler\n\nFind your login controller/screen and update it to save the donor ID:\n\n```dart\n// In your login/auth screen or service\nimport '../services/storage_service.dart';\n\n// After successful login\nif (response.statusCode == 200) {\n  final data = jsonDecode(response.body);\n  final token = data['data']['token'];\n  final donorId = data['data']['donorId'];  // Extract donor ID\n  \n  // Save both token and donor ID\n  await StorageService.saveToken(token);\n  await StorageService.saveDonorId(donorId);  // ✅ NEW\n  \n  // Navigate to home\n  Navigator.pushReplacementNamed(context, '/home');\n}\n```\n\n## Step 2: Initialize Socket in Emergency Screen\n\nThe emergency screen now automatically initializes socket on load:\n\n```dart\n// Already implemented in emergency_screen.dart\n// - Initializes socket on initState\n// - Registers emergency alert callback\n// - Displays real-time alerts\n// - Shows connection status indicator\n```\n\n## Step 3: Run Pub Get\n\n```bash\ncd mobile-app\nflutter pub get\n\ncd blood_app\nflutter pub get\n```\n\n## Step 4: Test Real-Time Alerts\n\n### Quick Test via Backend\n\n```bash\n# Test broadcast emergency (all O+ donors)\ncurl -X POST http://localhost:5000/api/notifications/broadcast \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Authorization: Bearer YOUR_TOKEN\" \\\n  -d '{\n    \"bloodType\": \"O+\",\n    \"message\": \"Test emergency\",\n    \"hospitalName\": \"City Hospital\"\n  }'\n\n# Test broadcast nearest (within 50km)\ncurl -X POST http://localhost:5000/api/notifications/broadcast-nearest \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Authorization: Bearer YOUR_TOKEN\" \\\n  -d '{\n    \"bloodType\": \"O+\",\n    \"hospitalId\": 1,\n    \"radiusKm\": 50,\n    \"message\": \"Urgent nearby request\"\n  }'\n```\n\n## Step 5: Verify Connection\n\nOpen emergency screen and check:\n- ✅ AppBar shows cloud icon (connected) or loading spinner (connecting)\n- ✅ Background shows no console errors\n- ✅ Console logs show: \"[Emergency Screen] Socket listener initialized\"\n\n## Step 6: Test Emergency Alert\n\n1. Keep emergency screen open\n2. Trigger broadcast from hospital staff\n3. Verify:\n   - Alert appears at top of list\n   - Pop-up notification shows\n   - Connection icon appears in AppBar\n   - Can click Accept/Decline buttons\n\n## Troubleshooting\n\n### Socket Not Connecting\n```dart\n// Check in emergency screen\nprint(NotificationService.isConnected()); // Should be true after 2-3 seconds\n\n// Check donor ID is saved\nfinal donorId = StorageService.getDonorId();\nprint('Donor ID: $donorId');  // Should not be null\n```\n\n### Alerts Not Appearing\n1. Check donor notification consent is `true`\n2. Verify blood type matches in broadcast\n3. Check backend logs: `[Emergency Broadcast] Starting to...`\n4. Verify donor is connected: Check socket connection count\n\n### Connection Drops\n- Automatically reconnects on poor network\n- Check backend socket server is running\n- Verify CORS settings allow your app's domain\n\n## API Response Format\n\n### Login Response (Updated)\n```json\n{\n  \"success\": true,\n  \"message\": \"Login successful\",\n  \"token\": \"eyJhbGc...\",\n  \"data\": {\n    \"userId\": 123,\n    \"donorId\": 456,\n    \"role\": \"donor\",\n    \"name\": \"John Doe\",\n    \"phone\": \"+1234567890\"\n  }\n}\n```\n\n### Emergency Alert Event\n```json\n{\n  \"id\": \"alert-uuid\",\n  \"donorId\": 456,\n  \"title\": \"🚨 Emergency Blood Request\",\n  \"message\": \"Hospital needs O+ blood\",\n  \"type\": \"EMERGENCY\",\n  \"urgency\": \"high\",\n  \"bloodType\": \"O+\",\n  \"read\": false,\n  \"response\": \"PENDING\",\n  \"createdAt\": \"2026-05-28T10:30:00Z\",\n  \"timestamp\": \"2026-05-28T10:30:00Z\"\n}\n```\n\n## Key Files Modified\n\n| File | Change |\n|------|--------|\n| `pubspec.yaml` | Added socket_io_client, firebase_messaging |\n| `lib/services/notification_service.dart` | Socket listener + callbacks |\n| `lib/services/storage_service.dart` | Added getDonorId/saveDonorId |\n| `lib/screens/emergency_screen.dart` | Socket init + real-time updates |\n| `backend/controllers/auth.controller.js` | Added donorId to login response |\n| `backend/sockets/socket.js` | Donor registration system |\n| `backend/controllers/notification.controller.js` | Socket event emissions |\n\n## Optional: Firebase Push Notifications\n\nFor devices without socket connection (background mode):\n\n1. Set up Firebase in your Flutter project\n2. Implement push notification handler\n3. Backend will fall back to push if socket unavailable\n\n```dart\n// In main.dart or app initialization\nimport 'package:firebase_messaging/firebase_messaging.dart';\n\nFirebaseMessaging.onMessage.listen((RemoteMessage message) {\n  // Handle incoming notification\n  print('Got a message: ${message.data}');\n});\n```\n\n## Production Checklist\n\n- [ ] Backend socket server deployed and accessible\n- [ ] Mobile app updated with socket_io_client\n- [ ] Donor login saves donorId to storage\n- [ ] Emergency screen initializes socket on load\n- [ ] Network security configured for WebSocket\n- [ ] Firebase push notifications set up (optional)\n- [ ] Rate limiting configured on broadcasts\n- [ ] Monitoring enabled for broadcast metrics\n- [ ] Testing with real emergency scenario\n\n## Support\n\nFor issues, check:\n- Backend logs: Look for `[Socket]` and `[Emergency Broadcast]` entries\n- Mobile logs: Check console for `[Socket]` debug messages\n- Database: Verify notifications table has records\n- Network: Check firewall allows WebSocket connections\n\n---\n\n**Status**: Ready for Production ✅\n**Last Updated**: May 28, 2026\n